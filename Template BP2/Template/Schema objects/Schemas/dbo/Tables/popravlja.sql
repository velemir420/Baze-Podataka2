CREATE TABLE popravlja (
    domar_id_radnik       INTEGER NOT NULL,
    prijavljuje_id_soba   INTEGER NOT NULL,
    prijavljuje_jmbg      CHAR(13) NOT NULL,
    prijavljuje_id_kvar   INTEGER NOT NULL
);
